import TimesList from './TimesList';
function Times() {
    return (
        <section className="timeCont subPage">
            <h2>TimesList</h2>
            <TimesList />
        </section>
    );
};

export default Times;